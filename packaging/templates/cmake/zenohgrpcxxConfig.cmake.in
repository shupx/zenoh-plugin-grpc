include(CMakeFindDependencyMacro)
include(GNUInstallDirs)

set(_zenohgrpcxx_targets "${CMAKE_CURRENT_LIST_DIR}/zenohgrpcxxTargets.cmake")
if(EXISTS "${_zenohgrpcxx_targets}")
    include("${_zenohgrpcxx_targets}")
endif()
